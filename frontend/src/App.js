import React from 'react';
import './App.css'; // Assuming App.css is in the same directory as App.js
// Ensure the path below is correct based on where you saved the Homepage component
import Homepage from './Pages/Homepage'; // Corrected import path for Homepage

function App() {
  return (
    // Render the Homepage component here
    // The Homepage component already contains the full layout and styling.
    <Homepage />
  );
}

export default App;
